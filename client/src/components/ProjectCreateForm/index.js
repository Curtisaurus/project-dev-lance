import React from 'react';
import { CREATE_PROJECT } from '../../utils/actions';

function CreateProject(project){
    const {
        _id,
        name,
        owner,
        descrtiption,
        image,
        redFunds,
        acqFunds,
        
    } = project

    return (
        //Render Project Component
        <div>

        </div>
    );
}

export default CreateProject;